This `coverage/` folder contains generated coverage reports and is safe to remove from the repo.
It has been archived to `dev-tools/archived/coverage/user-service` for reference.
Remove it if you don't need the historical coverage report artifacts.
